# WP-27 — Golden Workflow Regression / Feature Freeze

## Status

`HUMAN_ACCEPTED / PASS / COMPLETE` under G28. Allocation: `3` points. Human
decision: `G28_DECISION=ACCEPT_AND_COMMIT_PUSH`.

## Scope

Protect the four canonical V1 Golden Workflow paths and the relevant failure
boundaries before the feature freeze: success execution, hard-gate behavior,
partial participant failure isolation, restart/reload durability, timeout and
cancel cleanup, and truthful evidence/result handling.

## Verification

The affected Core command below passed all `40` tests (exit `0`), including the
four canonical workflow load/execute regressions and failure-injection,
resource-guard, evaluation-metric, and loader checks:

`C:/temp_pn_venv2/Scripts/python.exe -m pytest -q -rA services/core/tests/test_cp05_wp27_golden_workflows.py services/core/tests/test_cp06_wp28_failure_injection.py services/core/tests/test_wp24_resource_guards.py services/core/tests/test_wp25_evaluation_metrics.py services/core/tests/test_workflow_loader.py`

The browser-companion boundary suite passed `10/10` (exit `0`). The Web suite
passed `85/85` (exit `0`) and production build passed (exit `0`). The controlled
browser fixture passed `4/4` assertions (exit `0`) and cleanup PASS. Evidence is
stored under `artifacts/verification/g28-ux-feature-freeze-20260903/`.

## Feature-freeze boundaries

No workflow definition, Core contract, persistence migration, Runtime identity,
vendor integration, credential, cookie, token, cloud fallback, or automatic
external send was added. Live authenticated vendor verification remains gated
to G29. G28 must stop for Human decision after independent review; it must not
start G29.
