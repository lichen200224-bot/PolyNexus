# WP-15 — OpenCode Runtime Adapter Conformance

## G25 integrated candidate status — 2026-09-02

- `TASK_ID`: `G25-RUNTIME-ADAPTERS-AND-DOCTOR-ACCEPTANCE / WP-15`
- `STATUS`: `HUMAN_ACCEPTED / COMMIT_PENDING`; component history below is provenance,
  not a substitute for this combined-lane review.
- `WRITER`: Codex; `REVIEWER`: Carver, distinct bounded read-only reviewer,
  returned `PASS` with `BLOCKER=0`, `MAJOR=0`, `MINOR=0`.
- Candidate evidence: `tests/test_wp15_opencode_runtime.py` — 36 passed, exit 0;
  Full Core exit 0. The adapter remains local deterministic `EXPERIMENTAL`.
- Combined-lane integration leaves OpenCode out of the default execution
  registry and makes no live CLI/network/account or production-support claim.
  Human authorization is recorded; final +5 remains pending checkpoint
  verification after commit, push, and clean clone.
- `ADR_IMPACT`: ADR-011 preserved; ADR-012 integration addendum applies.

## Task status

- TASK_ID: WP15-OPENCODE-RUNTIME-ADAPTER
- STATUS: `HUMAN_ACCEPTED / CROSS_MACHINE_CONTINUATION_READY`
- ATTEMPT: 3
- BRANCH: wp15-opencode-runtime (isolated worktree)
- BASELINE HEAD: `c74a69629708f5c4dd03d69fca577ff70cf73297` (PRE-WP14-B acceptance checkpoint)
- CURRENT FIXED REVISION: product checkpoint `3fd75a289d4e1ceca63cdd326fad7d292f692b33` on `backup/wp15-opencode-runtime`; canonical 65c6582 narrow `_get` fix
- WRITER: OpenCode
- REVIEWER: Fresh independent Codex context (Writer != Reviewer) — `CODEX_VERIFIED_PASS / NO BLOCKER` (Attempt 3)
- ANTIGRAVITY_STATUS: NOT_REQUIRED — no UI/browser/E2E surface
- NEXT_OWNER: G06 parallel read-only review; no further WP-15 product changes authorized
- HUMAN_AUTHORIZATION: Human explicitly authorized the exact 3-file stage/commit/push gates and final WP-15 acceptance in this turn; no other files, branches, or G04 actions authorized
- ADR_IMPACT: None — ADR-001～ADR-011 decision text unchanged; adapter-only narrow _get fix within ADR-011 §5/§8
- DEPENDENCY: PRE-WP14-A cleanup verification machinery (`RunSupervisor._cleanup_and_verify`); PRE-WP14-B runtime binding contract

## Attempt 3 (this attempt — unknown-reference exception-chain secret leakage)

G05 reconciliation FIX_REQUIRED: `_get()` leak via `__cause__/__context__/traceback`.

Fix applied (canonical 65c6582 narrow mode; only 2 product files changed; task-state documentation is the third allowlisted file):

1. `services/core/src/polynexus_core/runtime/opencode.py` — `_get()` now:
   `exc = None; try: return self._runs[runtime_ref] except KeyError: exc = KeyError(_UNKNOWN_REF_ERROR); raise exc`
   Public-safe KeyError is constructed inside the `except` but raised only after leaving the raw-KeyError context, so `__cause__ is None`, `__context__ is None`, and formatted traceback contains no caller-controlled marker.
2. `services/core/tests/test_wp15_opencode_runtime.py` — `test_unknown_reference_fails_closed_without_echo` now parametrized per method (`status/result/cancel/cleanup/artifacts/resume`) and asserts safe public message, `marker not in str(exc)`, `marker not in repr(exc.args)`, `exc.__cause__ is None`, `exc.__context__ is None`, and `marker not in traceback.format_exception(exc)`. Also retains `await super().cleanup` fix from Attempt 2.
3. No change to Registry, contracts, supervisor, domain, persistence, API, migration, G03/G04/WP-14/WP-16 or main-workspace dirty files.

### Verification (actual commands, Attempt 3 — Writer side, isolated worktree `runtime-data/wp15-opencode-runtime`)

Launcher: `C:\temp_pn_venv2\Scripts\python.exe -B`. All commands run in the isolated worktree at branch `wp15-opencode-runtime`, HEAD `c26e10b`.

| Command | Result | Exit code |
|---|---|---|
| `pytest -q -rA --disable-warnings services\core\tests\test_wp15_opencode_runtime.py` | 36 PASSED (6 unknown-ref + 6 poisoned-subclass cases), no warnings | 0 |
| `pytest -q -rN --disable-warnings services\core\tests\test_runtime_skeleton.py services\core\tests\test_wp14b_runtime_binding.py` | 101 passed | 0 |
| `pytest --collect-only -q --disable-warnings services\core` | 509 collected | 0 |
| `pytest -q -rN --disable-warnings services\core` | 508 passed, 1 skipped | 0 |
| `python -B scripts\validate_baseline.py` | PASS: 11 required files; workflows valid; MV3 manifest valid | 0 |
| `tools\validate-polynexus-governance.ps1` | PASS: SOP v1.1 governance valid | 0 |
| `git diff --check` | clean | 0 |

Known non-fatal Windows atexit `PermissionError` (`pytest-of-hikar\pytest-current`) reproduced; all exit codes 0.

### Independent verification and final acceptance (Human-authorized)

- Fresh independent Codex re-review: `CODEX_VERIFIED_PASS / NO BLOCKER`; the Attempt-3 product change was reviewed after the prior c26 review and was not accepted from historical self-report.
- Product checkpoint: `backup/wp15-opencode-runtime@3fd75a289d4e1ceca63cdd326fad7d292f692b33`, pushed with the exact three-file allowlist; push exit code `0`.
- Fresh clean-clone: `artifacts/verification/clean-clone-wp15-3fd75a2-20260828`, branch `wp15-opencode-runtime`, HEAD exactly `3fd75a289d4e1ceca63cdd326fad7d292f692b33`, clean before and after verification.
- Fresh candidate targeted WP-15: 36 passed, exit code `0`.
- Fresh clean-clone baseline validator: PASS, exit code `0`.
- Fresh clean-clone governance validator: PASS, exit code `0`.
- Fresh clean-clone `git diff --check`: clean, exit code `0`; staged and untracked file lists empty.
- HUMAN_ACCEPTANCE: `HUMAN_ACCEPTED` — explicit Human instruction authorized the exact checkpoint and final acceptance after the fresh clean-clone evidence.
- CROSS_MACHINE_CONTINUATION_READY: `CROSS_MACHINE_CONTINUATION_READY` for the product checkpoint above. This does not certify real OpenCode CLI/network/credentials/production integration.

## Attempt 2 (Codex Attempt-1 review remediation — historical)

Fresh independent Codex review of Attempt 1 returned `VERIFIED_PASS` on the product implementation with zero code blockers and one MINOR residual finding: this task doc still labeled ATTEMPT 1 / Attempt-1 verification, making current evidence inconsistent with handoff state.

Fix applied (Human-authorized docs-only sync; only this file changed in Attempt 2):

1. `_CleanupFalseOpenCode.cleanup()` in `services/core/tests/test_wp15_opencode_runtime.py` now correctly awaits `super().cleanup(runtime_ref)` while keeping `return False`, preserving supervisor fail-closed `CANCEL_REQUESTED -> ORPHANED` coverage. (Applied at the start of Attempt 2 under the Human FIX_PROMPT.)
2. This document updated to Attempt 2 state; Attempt 1 retained below as historical evidence.
3. No change to `runtime/opencode.py`, contracts, registry, supervisor, domain, persistence, API or migrations.

### Verification (actual commands, Attempt 2 — Writer side)

Launcher: `C:\temp_pn_venv2\Scripts\python.exe -B`. All commands run in the isolated worktree at branch `wp15-opencode-runtime`, HEAD `c74a696`.

| Command | Result | Exit code |
|---|---|---|
| `pytest -q -rA services\core\tests\test_wp15_opencode_runtime.py` | 25 PASSED (incl. `_CleanupFalseOpenCode` timeout/cancel params), no warnings | 0 |
| `pytest -q -rA --disable-warnings services\core\tests\test_runtime_skeleton.py services\core\tests\test_wp14b_runtime_binding.py` | 101 passed (machine-counted PASSED lines), 0 FAILED/ERROR/SKIPPED | 0 |
| `pytest -q -rN --disable-warnings services\core` | 503 passed, 1 skipped | 0 |
| `git diff --check` | clean | 0 |

Known non-fatal Windows atexit `PermissionError` (`pytest-of-hikar\pytest-current`) reproduced; all exit codes 0.

### Independent verification (fresh Codex, Attempt 2 re-acceptance)

| Check | Result |
|---|---|
| Product implementation verdict | `VERIFIED_PASS`; Attempt-2 fix confirmed effective (`await super().cleanup(...)` verified) |
| Code blockers | None |
| WP-15 targeted | 25 passed, exit 0 |
| PRE-WP14-A/B regression | 101 passed, exit 0 |
| Full Core | 503 passed / 1 skipped, exit 0 |
| Collect-only | 504 collected, exit 0 |
| Baseline validator | PASS, exit 0 |
| Governance validator | PASS, exit 0 |
| `git diff --check` | clean, exit 0 |
| staged / tracked diff | 0 / 0 |

Reviewer-environment note: first launcher start inside the Windows sandbox exited 101; the reviewer re-ran successfully in a controlled environment outside the sandbox and did NOT mislabel the launcher failure as PASS. The single symlink-policy test remains SKIPPED.

## Attempt 1 (historical evidence)

## Scope implemented

Exact allowlist (3 files):

1. `services/core/src/polynexus_core/runtime/opencode.py` — NEW: `OpenCodeRuntimeAdapter`, deterministic no-network conformance adapter for the OpenCode runtime identity.
2. `services/core/tests/test_wp15_opencode_runtime.py` — NEW: 25 targeted conformance tests.
3. `docs/tasks/WP-15.md` — NEW: this document.

Conformance contract implemented (mirrors the accepted WP-14 Codex adapter pattern):

- **Protocol**: satisfies the frozen `RuntimeAdapter` Protocol unchanged; all 12 methods with matching sync/async signatures.
- **Lifecycle**: `create_run` → `opencode:<uuid4-hex>` refs (unique); `submit` only from CREATED → RUNNING; `status`/`result` normalization; result only from RUNNING/COMPLETED.
- **Cancel / timeout cleanup**: follows the PRE-WP14-A shared machinery semantics — `cancel()` stops only active states (CREATED/STARTING/RUNNING) and leaves terminal states (including TIMED_OUT) unchanged so `RunSupervisor._cleanup_and_verify` post-cleanup status observes exactly TIMED_OUT (timeout) or CANCELLED (cancel); `cleanup()` is idempotent, returns True, and status stays queryable after cleanup.
- **Result/error normalization**: FAILED status exposes only the constant `_NORMALIZED_FAILURE_ERROR` ("OpenCode runtime reported execution failure"); raw vendor/exception text never enters `RuntimeStatus.error`, `RuntimeResult.summary`, events, or Evidence metadata. Result summary is a constant; no fabricated Evidence/Finding/Artifact (supervisor owns durable evidence).
- **Capabilities** (truthful): cancel=True, resume=ResumeMode.NONE (native resume never faked), artifacts=True, timeout_cleanup_verified=True, usage_visibility=UNAVAILABLE, auth_ownership=NONE.
- **Version identity**: stable constant `opencode-local-adapter/0.1`.
- **Unknown references fail closed** without echoing caller-supplied reference material.
- **No vendor-specific branch in Core**: static guard test asserts registry/supervisor/workflows.execution source contains no "opencode"; built-in registry still registers only `builtin.reference`. The adapter is NOT registered in the default registry (composition-root registration is out of scope for this allowlist).

## Secret handling

No credential, token, cookie, session, or endpoint secret is stored, derived, logged, or echoed. Tests inject a marker (`SECRET_MARKER_WP15_DO_NOT_PERSIST`) into caller input and adversarial exception messages and assert it never reaches any adapter-derived or supervisor-persisted surface.

## Verification (actual commands, Attempt 1)

Launcher: `C:\temp_pn_venv2\Scripts\python.exe` (Python 3.13 venv, same controlled launcher as PRE-WP14-B). All commands run in the isolated worktree at branch `wp15-opencode-runtime`, HEAD `c74a696`.

| Command | Result | Exit code |
|---|---|---|
| `python -B -m pytest -q --disable-warnings services\core\tests\test_wp15_opencode_runtime.py` | 25 passed | 0 |
| `python -B -m pytest -q --disable-warnings services\core\tests\test_runtime_skeleton.py services\core\tests\test_wp14b_runtime_binding.py` | 101 passed (PRE-WP14-A/B regression) | 0 |
| `python -B -m pytest -q --disable-warnings -rN services\core` | 503 passed, 1 skipped | 0 |
| `python -B -m pytest --collect-only -q --disable-warnings services\core\tests\test_wp15_opencode_runtime.py` | 25 collected | 0 |
| `git diff --check` | clean | 0 |

The known non-fatal Windows atexit `PermissionError` (`pytest-of-hikar\pytest-current`, WinError 5) appeared during interpreter exit in the full-suite run; every command returned exit code 0. It is the same environment warning documented in PRE-WP14-B.

### PASS / SKIPPED / UNVERIFIED

- PASS: all items in the verification table above (this round, real commands, actual exit codes).
- SKIPPED: exactly 1 — the pre-existing WP-07 Windows symlink policy test (labeled SKIPPED, not PASS; pre-existing before this task).
- UNVERIFIED at Attempt 1 time: fresh independent Codex re-review verdict (superseded by the Attempt-2 VERIFIED_PASS above).

## Protected areas respected

Not modified: `runtime/contracts.py`, `runtime/registry.py`, `runtime/supervisor.py`, `runtime/reference.py`, `workflows/execution.py`, `domain/*`, `persistence/*`, `API/*`, `migrations/*`, `docs/15_DOCUMENT_INDEX.md`, `docs/tasks/WP-12.md`, and everything else outside the 3-file allowlist.

Isolation note: original working tree `feature/first-vertical-slice` (with its pre-existing dirty protected files `docs/15_DOCUMENT_INDEX.md`, `docs/tasks/WP-12.md` and untracked WP-14 files) was NOT touched; all WP-15 work lives in an isolated worktree on branch `wp15-opencode-runtime`.

## Known limitations

- Deterministic local adapter: no real OpenCode CLI/process/network execution path; conformance-level only.
- Adapter is not registered in `build_default_registry()`; composition-root wiring for an OpenCode profile requires a separate gated task (registry.py is protected here).
- Application log capture (logging handlers/stdout/stderr/caplog): NOT_IN_SCOPE — no logging surface in the adapter.
- API/export surfaces: NOT_IN_SCOPE — no new API endpoint or export mechanism in this allowlist.
- Leftover non-git artifacts inside the worktree's gitignored `runtime-data/` directory: `wp15_full_run.txt`, `wp15_reg.txt`, `wp15_full2.txt` (diagnostic logs; shell delete blocked by tool permissions; invisible to git status).

## Unverified items

- No remaining WP-15 checkpoint or clean-clone verification is unverified for product checkpoint `3fd75a289d4e1ceca63cdd326fad7d292f692b33`; future changes require a new independent review and Human Git gate.

## Do not change

- Do not modify `runtime/contracts.py`, `runtime/registry.py`, `runtime/supervisor.py`, domain, persistence, API, or migrations to accommodate this adapter.
- Do not absorb the excluded dirty files (`docs/15_DOCUMENT_INDEX.md`, `docs/tasks/WP-12.md`) or the untracked WP-14 files from the original working tree into this branch.
- No real/user database migration or production dependency install. No further push/remote changes are authorized outside the Human-approved checkpoint above.
