# WP-22 — Built-in Workflow Templates

## Status

`HUMAN_ACCEPTED / PASS / CHECKPOINTED` under G27; accepted score `3/3`.
Independent review: PASS, no BLOCKER/MAJOR/MINOR findings.

## Scope and acceptance

- Maintain the nine V1 built-ins: Code Review, Release Validation, Bug/Incident
  Analysis, Technical Design Review, Requirement Review, Change Impact Review,
  Document Review, SOP Review, and Decision/Proposal Comparison.
- Each definition must pass YAML safe parsing, `schemas/workflow.schema.json`,
  and canonical `WorkflowDefinition` normalization with version `1`.
- Representative execution must use the existing workflow/runtime boundary.
- No arbitrary script nodes, loops, vendor branches, or new persisted workflow
  entity are introduced.

## Current evidence

- `services/core/tests/test_workflow_loader.py` validates the exact nine-file
  inventory, schema normalization, fixed V1 node set, representative executor
  paths, and arbitrary-node rejection.
- `services/core/tests/test_cp05_wp27_golden_workflows.py` executes four
  representative Golden flows through `RunSupervisor` and the reference
  adapter.
- Targeted combined suite: `40 passed`, exit `0`.
- Full Core rerun: `720 passed`, `1 skipped`, exit `0`.

## Changed files in G27

- Test inventory assertion in `services/core/tests/test_workflow_loader.py`.
- Existing YAML definitions and loader/model/execution implementation were
  verified unchanged from the approved predecessor.

## Boundaries

`ADR_IMPACT=NONE`; `SCOPE_DEVIATION=NONE`. Workflow maturity remains V1
declarative/local and does not claim external-vendor execution.

## Unverified / limitations

Visual workflow authoring, distributed scheduling, arbitrary scripting, and
live vendor execution remain out of scope; G29 owns authenticated vendor work.
