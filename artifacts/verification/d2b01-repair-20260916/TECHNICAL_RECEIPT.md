# D2B-01 repair technical evidence

Reviewed parent: `d21fb54f782a3044d43082209394a583af69a24a`. Architecture decision: `D2B01-ARCH-01-RUNTIME-DISPATCH-AUTHORIZATION`. This is a technical repair Candidate for independent ChatGPT acceptance; it does not declare that acceptance or Human UAT.

F001: the default static Registry still invokes a zero-argument factory. Its OpenCode composition factory resolves the authoritative `POLYNEXUS_CONTENT_ROOT` through Core `ContentStore`. Tests cover a usable adapter under valid Core configuration, missing/invalid root failure, and no `reference.local` fallback. RuntimeRegistry has no vendor branch.

F002: `production-run.json` records an isolated synthetic Project, Task, Generation and exact Run. The first product start persisted `runtime.routing_policy` with `APPROVAL_REQUIRED`/`NEED_ACTION`, left Run `CREATED`, and created no binding or ACP process. A distinct `TEST_ONLY` exact-Run authorization was issued in that isolated DB, then `ExecutionService.execute_existing_run` retried the same Run. The independent SQLite read at actual owned Job creation (`launch_observations_before_effect`) saw Run `STARTING`, selected binding `opencode.acp.local`, authorization `CONSUMED`, original policy `APPROVAL_REQUIRED`, and one separate dispatch Evidence. This read happens before the real OpenCode process is created. `production-readback.json` independently confirms one consume event, terminal `COMPLETED`, Generation `closed=1`, one exact-Run Core Artifact, routing-policy/dispatch/runtime Evidence, and unchanged original selected source.

The target is real OpenCode ACP v1, executable version `1.18.31`, and session `currentValue=opencode/mimo-v2.5-free` in `production-acp.stdout.jsonl`. Its process exited 0 with verified owned stop. Runtime Evidence records `PROJECTED_STAGING`, no enabled plugins/MCP, source changed path `bug.py`, executable/version and exit. The selected `bug.py` hash changed from `a88461f8368a8e884152e5d38bc7a52315f9e311d6c094597c3ad15700bf3911` to `232ebf42bfe8ca6cb387145693368b0708b36fd6d25d10c5949a60afdb9ff473` in staging; original selected source and `test_bug.py` are unchanged. Independent postcondition pytest exited 0 (`1 passed`). Core blob Artifact content SHA-256 equals its persisted SHA-256 `7749dc5c1111dfc60c7ec37c1133739e3e4f07f6c3202834b61354243f74869b` and binds the exact Run.

The earlier first real product-path attempt is preserved as `first-timeout-production-run.json` with actual command exit 1. The original Core 30-second workflow deadline timed out before the target returned a prompt/tool result; Core owned cleanup stopped that process, and the Run terminal state was `TIMED_OUT`. The corrected run uses a bounded Core-owned 120-second OpenCode operation deadline. The earlier failed run and the corrected successful run are separate isolated diagnostic identities; no failure evidence was removed or relabelled.

The completed run's original console line `PRODUCTION_PATH_BINDING=NONE` printed the *initial* preapproval binding variable. The same run's `production-run.json` contains its final persisted binding, and `production-readback.json` plus the independent `launch_observations_before_effect` verify that binding before process creation. The diagnostic runner now prints the final binding on future executions; the original console output remains unchanged here.

F003: direct adapter tests exercise active exchanges for both `CANCELLED` and `TIMED_OUT` cleanup targets. They prove exact runtime reference, exchange cancellation, owned process stop, verified cleanup, and no source/result import after cancellation. This claims Core-owned process cancellation, not native provider cancellation.

The original ACP `availableCommands` and declared MCP capability do not prove live extension egress denial. The extension-egress limitation remains open until independently exercised. The existing Windows nested-reparse symlink test remains `SKIPPED` because this environment cannot create its symlink. `TEST_ONLY` dispatch authorization is technical execution evidence, never production Human approval or Candidate Human Accept; the Human-facing Run UX is deferred to D4.
