# D2B-01 technical Candidate receipt

Base product commit: `1fd856d3016ff0e155f97af3d5fd74ce2b368e6b`. The new opt-in `opencode.acp.local` profile uses the existing Core RuntimeAdapter, Registry, RunSupervisor, PROJECTED_STAGING, ContentStore and Artifact/Evidence authority. It adds a generic ACP v1 stdio transport and a thin OpenCode adapter. The legacy deterministic OpenCode profile remains independently registered; no runtime fallback is implemented.

The real installed `opencode-ai` executable was version `1.18.31`, SHA-256 `0242a0dc705af67c90882b456a36b619883c1c786aad8fe071a1bc64e5d1d440`. The real target `real-run.json` includes actual executable/argv/cwd, retained Windows Job facts, ACP terminal, child exit 0, changed path `bug.py`, unchanged original source, before/after hashes, baseline pytest exit 1 and independent postcondition pytest exit 0. `acp.stdout.jsonl` is the sanitized raw ACP exchange: 3 JSON-RPC responses and 51 `session/update` records, with actual `read`, `edit`, `read` tool calls. `source-change.diff` and `source-before`/`source-after` allow independent source comparison. The real idle control separately records a 0.5-second read timeout and verified owned termination; its child exit is 1.

OpenCode authentication was runtime-managed through already present official OpenCode provider credentials. The adapter does not accept a SecretRef value or pass credential material in argv/config. It pins the free model `opencode/mimo-v2.5-free`; ACP session model `currentValue` was observed as that model. The child starts with `--pure`, empty plugin/MCP sets, isolated config, deny-by-default permissions and no Core-advertised filesystem/terminal ACP capabilities. Provider-model egress is `RUNTIME_MANAGED`; agent extension egress is `DENY`. Native resume is `NONE`.

Negative contract tests in `test_d2b01_opencode_acp.py` cover missing executable, unsupported ACP version, malformed/timeout/mismatched response, wrong session notification, unsupported agent request, absent/wrong model state, config/policy drift, traversal, non-allowlisted output, missing or wrong source/postcondition, nonzero child exit, import-time source mutation, secret-like prompt, undeclared extension permissions, unexpected plugin enablement and no implicit runtime fallback. Fixture negatives establish Core fail-closed logic; only `real-run.json`, `acp.stdout.jsonl` and `idle-control.json` establish actual OpenCode executable behavior.

Targeted tests: `20 passed`, exit 0. Affected regression: `83 passed, 1 skipped`, exit 0. Full Core on final source: `934 passed, 1 skipped`, exit 0; the earlier full Core run with the interactive timeout exception fix made during execution also completed `934 passed, 1 skipped`, exit 0. Both logs are preserved. The skip is `test_d2a_external_runtime.py::test_projected_staging_rejects_nested_reparse_path` because this Windows test environment could not create a symlink; it is not a PASS. Web tests/build were not required because no public API or UI files or contracts changed.

Known limits: no native resume claim; no independent Human acceptance claim; actual live extension network denial is based on isolated effective config and observed real tool records, not a separate live egress attempt. The separate real idle control proves owned stop, while the Core supervisor timeout path remains covered by existing Core regression tests. ChatGPT remains the independent D2B-01 acceptance center.
