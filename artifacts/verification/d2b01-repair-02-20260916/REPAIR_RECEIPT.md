# D2B-01 F004 exact-source technical receipt

The reviewed Candidate `237d5fc6b8495c3a455e1d2ba5cca7b2351c3441` contains accepted product source but a diagnostic final oracle that subscripts `ControlledJob.facts()` as a dict. The runner also printed an initial binding in its older real output. This repair changes only `tools/run_d2b01_production.py` and publishes a new evidence package; the original evidence is retained.

Commit A1 `4b1c2c57b78ad8e55833080400819c591133bf54` fixed process-fact list handling and printed the actual persisted binding. Its source was frozen and pushed before a real isolated run. That run's Core path reached `COMPLETED`, showed `APPROVAL_REQUIRED`, persisted `opencode.acp.local`, produced a postcondition exit 0, and independently read back successfully. Its *runner* exited 1 because its final authorization-event oracle also subscripted an ORM event row. A1 was not amended or rebased. Its command record and raw Run/ACP/read-back outputs remain here.

The additional immutable runner-only commit A2 `98e674570f357b4e8af23d9f203f993f42ce638e` corrected that event-row accessor. A2 was pushed and frozen with a clean worktree before a completely new real run. The exact A2 runner exited 0 and printed `PRODUCTION_PATH_OUTCOME=COMPLETED`, `PRODUCTION_PATH_POLICY=APPROVAL_REQUIRED`, `PRODUCTION_PATH_BINDING=opencode.acp.local`, `PRODUCTION_PATH_AUTHORIZATION=CONSUMED`, and `PRODUCTION_PATH_POSTCONDITION_EXIT=0`. The independently committed read-back tool exited 0. `SOURCE_PROVENANCE.json` binds A2's commit/tree and runner/read-back/executable hashes to those command exits. The publication commit has A2 as its parent and contains evidence only.

The immutable A2 runner was also read from GitHub raw HTTP (status 200); its remote SHA-256 exactly matched the pre-run `runner_sha256` in `SOURCE_PROVENANCE.json`.

The new DB read-back verifies Run `COMPLETED`, Generation closed, the exact OpenCode binding, authorization `CONSUMED` with one CONSUME event, routing-policy Evidence `APPROVAL_REQUIRED/NEED_ACTION`, separate dispatch and `runtime.opencode.acp` Evidence, one exact-Run Core Artifact, unchanged original selected source and only the expected staging change. The runner's `all_evidence_sources` matches the independent DB set. ACP raw output identifies real OpenCode 1.18.31 and the configured `opencode/mimo-v2.5-free` model; the retained process fact exited 0 and is stopped.

Previously reviewed F001 factory, F003 active cancellation and the RuntimeDispatchAuthorization product contract are carried forward without source or test changes. The nested-reparse symlink case remains `SKIPPED` in this Windows environment. Live extension-egress denial has not been independently exercised, and Human-facing dispatch approval UX/UAT remains deferred to D4 / Final Human UAT. This package is technical evidence for ChatGPT review, not a declaration of ChatGPT acceptance or Human approval.
